import { requireNativeComponent } from "react-native";

/**
 * This file must be imported ONLY as a component,
 * never re-required elsewhere.
 */
const NativeWebPImage = requireNativeComponent("NativeWebPImage");

export default NativeWebPImage;